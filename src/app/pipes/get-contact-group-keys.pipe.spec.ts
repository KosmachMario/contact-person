import { GetContactGroupKeysPipe } from './get-contact-group-keys.pipe';

describe('GetContactGroupKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new GetContactGroupKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
